/**
 * `pdf/converter` — rasterise a PDF into PNG/JPEG images (Phase 3). Browser-only.
 */
export {
  convertPdfToImages,
  imageFileName,
  DPI_PRESETS,
  type ImageFormat,
  type ConvertPdfToImagesOptions,
  type ConvertedImage,
} from './convert.js';
